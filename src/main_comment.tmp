fn main() {
    // set(niveau_max, zones) → affiche les logs niveau <= niveau_max dans les zones actives
    // set(0,   zones) → aucun log
    // set(10,  zones) → info normale seulement (niveau <= 10)
    // set(100, zones) → tout afficher
    deep_log::set(10, LogZone::BASIC | LogZone::RENDER);
